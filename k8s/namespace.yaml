# Everything in this demo lives in one namespace.
# The SPIRE registration entry for the agent matches on this namespace +
# the "agent" ServiceAccount (see k8s/spire/ and scripts/setup.sh).
apiVersion: v1
kind: Namespace
metadata:
  name: agent-nhi
  labels:
    app.kubernetes.io/part-of: enterprise-agent-nhi
